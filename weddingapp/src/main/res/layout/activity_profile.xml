<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/download">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="10dp">

        <ImageView
            android:id="@+id/imgProfilePicture"
            android:layout_width="281dp"
            android:layout_height="94dp"
            android:layout_marginBottom="12dp"
            android:contentDescription="Profile Picture"
            android:scaleType="centerCrop"
            android:src="@drawable/log" />

        <!-- New button to change the profile picture -->
        <Button
            android:id="@+id/btnChangeProfilePicture"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change Profile Picture"
            android:layout_marginBottom="16dp"
            android:backgroundTint="@color/light_grey"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="cursive" />

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:fontFamily="cursive"
            android:gravity="center"
            android:text="Edit Couple's Information"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- Partner 1 Name -->
        <EditText
            android:id="@+id/etPartner1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Partner 1 Name"
            android:inputType="textPersonName"
            android:textSize="16sp"
            android:padding="12dp"
            android:background="@drawable/edit_text_background"
            android:layout_marginBottom="16dp"
            android:textColor="@color/white"
            android:textColorHint="@color/white" />

        <!-- Partner 2 Name -->
        <EditText
            android:id="@+id/etPartner2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Partner 2 Name"
            android:inputType="textPersonName"
            android:textSize="16sp"
            android:padding="12dp"
            android:background="@drawable/edit_text_background"
            android:layout_marginBottom="16dp"
            android:textColor="@color/white"
            android:textColorHint="@color/white" />

        <!-- Estimated Budget Input -->
        <EditText
            android:id="@+id/etBudget"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Estimated Wedding Budget"
            android:inputType="numberDecimal"
            android:textSize="16sp"
            android:padding="12dp"
            android:background="@drawable/edit_text_background"
            android:layout_marginBottom="16dp"
            android:textColor="@color/white"
            android:textColorHint="@color/white" />

        <!-- Wedding Date Button -->
        <Button
            android:id="@+id/btnWeddingDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:layout_marginBottom="24dp"
            android:backgroundTint="@color/light_grey"
            android:text="Select Wedding Date"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="cursive" />

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSaveProfile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:backgroundTint="@color/light_grey"
            android:text="Save"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="cursive" />

    </LinearLayout>
</ScrollView>
